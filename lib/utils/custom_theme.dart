/*
import 'package:flutter/material.dart';
import 'package:google_fonts/google_fonts.dart';

import 'custom_colors.dart';
import 'custom_font_style.dart';

class Styles {
  static ThemeData themeData(bool isDarkTheme, BuildContext context) {

    return ThemeData(
      */
/*textTheme: TextTheme(
        displayLarge: GoogleFonts.inter(
              textStyle: TextStyle(
                  fontWeight: FontWeight.bold,
                  color: isDarkTheme
                      ? CustomColors.whiteColor
                      : CustomColors.blackColor),
          ),
        displayMedium: GoogleFonts.inter(
      textStyle: TextStyle(
          fontWeight: FontWeight.bold,
          color: isDarkTheme
              ? CustomColors.whiteColor
              : CustomColors.pinkColor),
    ),
        displaySmall: GoogleFonts.inter(
      textStyle: TextStyle(
          fontWeight: FontWeight.bold,
          color: isDarkTheme
              ? CustomColors.whiteColor
              : CustomColors.greyColor),
    ),
      ),*//*

      iconTheme: IconThemeData(
          color:
              isDarkTheme ? CustomColors.whiteColor : CustomColors.blackColor),
      // brightness: isDarkTheme ? Brightness.dark : Brightness.light,
      appBarTheme: AppBarTheme(
          elevation: 0,
          color:
              isDarkTheme ? CustomColors.blackColor : CustomColors.whiteColor),
      scaffoldBackgroundColor:
          isDarkTheme ? CustomColors.blackColor : CustomColors.whiteColor,
      primaryColor:
          isDarkTheme ? CustomColors.whiteColor : CustomColors.pinkColor,
      indicatorColor:
          isDarkTheme ? CustomColors.whiteColor : CustomColors.pinkColor,
      hintColor: isDarkTheme ? Color(0xff280C0B) : Color(0xffEECED3),
      highlightColor: isDarkTheme
          ? CustomColors.whiteColor2Tint
          : CustomColors.pinkColorTint,
      hoverColor: isDarkTheme
          ? CustomColors.whiteColor2Tint
          : CustomColors.pinkColorTint,
      focusColor: isDarkTheme
          ? CustomColors.whiteColor2Tint
          : CustomColors.pinkColorTint,
      disabledColor: Colors.grey,
      // cardColor: CustomColors.blackColorTint,
      cardTheme: CardTheme(
        elevation: 0,
        color: isDarkTheme
            ? CustomColors.whiteColor2Tint
            : CustomColors.whiteColor,
        shape: RoundedRectangleBorder(
          borderRadius: BorderRadius.circular(10.0),
          side: BorderSide(
            color: isDarkTheme
                ? CustomColors.whiteColor2Tint
                : CustomColors.greyColor,
            //  width: 1.0,
          ),
        ),
      ),
      canvasColor: isDarkTheme ? Colors.black : Colors.grey[50],
      tabBarTheme: TabBarTheme(
          labelColor:
              isDarkTheme ? CustomColors.whiteColor : CustomColors.pinkColor,
          labelStyle: _customTextStyle.pink16W700(),
          indicatorColor: Colors.transparent,
          unselectedLabelColor:
              isDarkTheme ? CustomColors.greyColor : CustomColors.greyColor,
          unselectedLabelStyle: _customTextStyle.grey16W700()),
      bottomNavigationBarTheme: BottomNavigationBarThemeData(
        backgroundColor:
            isDarkTheme ? CustomColors.blackColor : CustomColors.whiteColor,
      ),
      elevatedButtonTheme: ElevatedButtonThemeData(
        style: ElevatedButton.styleFrom(
            elevation: 0,
            shape: RoundedRectangleBorder(
                //to set border radius to button
                borderRadius: BorderRadius.circular(6.0)),
            backgroundColor: CustomColors.pinkColor),
      ),
      checkboxTheme: Theme.of(context).checkboxTheme.copyWith(
          shape: RoundedRectangleBorder(
            borderRadius: BorderRadius.circular(20),
          ),
          side: BorderSide(
              width: 1.5,
              color: isDarkTheme
                  ? CustomColors.whiteColor
                  : CustomColors.pinkColor),
          splashRadius: 0),
      colorScheme: ColorScheme.fromSwatch(primarySwatch: Colors.pink).copyWith(
        secondary: isDarkTheme
            ? CustomColors.whiteColorTint
            : CustomColors.pinkColorTint,
        brightness: isDarkTheme ? Brightness.dark : Brightness.light,
      ),
      buttonTheme: ButtonThemeData(
          buttonColor:
              isDarkTheme ? CustomColors.whiteColor : CustomColors.pinkColor),
    );
  }
}
*/
